# Copy to `.env` (same folder as this file) and fill in your values.
# IMPORTANT: Do not commit `.env`.

# MongoDB connection string (local or Atlas)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-host>/<optional-db>?retryWrites=true&w=majority
